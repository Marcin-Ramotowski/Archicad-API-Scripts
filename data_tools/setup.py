from setuptools import setup, find_packages

setup(
    name='data_tools',
    version='1.0',
    packages=find_packages(),
    description='Set of useful tools which contains code to connect with instance of Archicad \
                 and set of functions used for data processing.'
)